name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Grant execute permission to gradlew
        run: chmod +x ./gradlew

      - name: Build with Gradle
        run: ./gradlew clean bootJar

      - name: Rename jar
        run: |
          JAR_NAME=$(find build/libs -name "*.jar" ! -name "*plain*" | head -n 1)
          cp "$JAR_NAME" app.jar

      - name: Copy jar to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "app.jar"
          target: "/home/ec2-user/app"

      - name: Restart application on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            APP_PID=$(pgrep -f '/home/ec2-user/app/app.jar' || true)
            if [ -n "$APP_PID" ]; then
              echo "Killing existing app pid: $APP_PID"
              kill -15 $APP_PID || true
              sleep 5
            fi

            REMAIN_PID=$(pgrep -f '/home/ec2-user/app/app.jar' || true)
            if [ -n "$REMAIN_PID" ]; then
              echo "Force killing remaining pid: $REMAIN_PID"
              kill -9 $REMAIN_PID || true
              sleep 2
            fi

            nohup java -jar /home/ec2-user/app/app.jar --spring.profiles.active=local > /home/ec2-user/app/app.log 2>&1 &
            sleep 5
            pgrep -af '/home/ec2-user/app/app.jar' || true